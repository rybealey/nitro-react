diff --git a/src/nitro/room/object/logic/MovingObjectLogic.ts b/src/nitro/room/object/logic/MovingObjectLogic.ts
index e6f85a11682bf5c882bd0d54bcdaa1c1447911a2..f8f3bda8f3f5b8d4cb81bff36f09cd24c68f8587 100644
--- a/src/nitro/room/object/logic/MovingObjectLogic.ts
+++ b/src/nitro/room/object/logic/MovingObjectLogic.ts
@@ -4,10 +4,10 @@ import { ObjectMoveUpdateMessage } from '../../messages';
 
 export class MovingObjectLogic extends RoomObjectLogicBase
 {
-    // Furniture and pets retain Nitro's existing 530ms jitter window. System
-    // 11 opts avatars into the server's exact shared 500ms walking cadence.
+    // PixelRP: the server normally emits a step every 500ms and may lengthen
+    // formation-grid convergence beats up to 530ms. Packet-anchored 530ms
+    // interpolation absorbs that variation without endpoint holds.
     public static DEFAULT_UPDATE_INTERVAL: number = 530;
-    public static SHARED_MOVEMENT_INTERVAL: number = 500;
     private static TEMP_VECTOR: Vector3d = new Vector3d();
 
     private _liftAmount: number;
@@ -75,9 +75,6 @@ export class MovingObjectLogic extends RoomObjectLogicBase
 
             if(difference === (this._updateInterval >> 1)) difference++;
 
-            // A packet received just before its nearest shared beat waits at
-            // the edge start; a late packet enters at the elapsed room phase.
-            if(difference < 0) difference = 0;
             if(difference > this._updateInterval) difference = this._updateInterval;
 
             if(this._locationDelta.length > 0)
@@ -146,19 +143,10 @@ export class MovingObjectLogic extends RoomObjectLogicBase
             store.set(this.object.instanceId, nowMs);
         }
 
-        if(this.useSharedMovementCadence)
-        {
-            // All avatar logics use the same room animation time. Quantizing
-            // to its nearest cadence beat removes permanent phase gaps caused
-            // by MASTER and JOINER packets arriving a few ms apart.
-            const cadence = MovingObjectLogic.SHARED_MOVEMENT_INTERVAL;
-
-            this._changeTime = (Math.round(this._lastUpdateTime / cadence) * cadence);
-        }
-        else
-        {
-            this._changeTime = this._lastUpdateTime;
-        }
+        // Start this edge from the frame that actually received it. System 11
+        // may share this smooth phase with a compatible JOINER, but it never
+        // rounds native avatar movement onto an unrelated client-clock beat.
+        this._changeTime = this._lastUpdateTime;
 
         this._locationDelta.assign(message.targetLocation);
         this._locationDelta.subtract(this._location);
@@ -174,11 +162,6 @@ export class MovingObjectLogic extends RoomObjectLogicBase
         return this._lastUpdateTime;
     }
 
-    protected get useSharedMovementCadence(): boolean
-    {
-        return false;
-    }
-
     protected set updateInterval(interval: number)
     {
         if(interval <= 0) interval = 1;
diff --git a/src/nitro/room/object/logic/avatar/AvatarLogic.ts b/src/nitro/room/object/logic/avatar/AvatarLogic.ts
index ef93437d2143055dfc9cac0032357348b2e272e9..66f7c7db09f74e6a91e9dbeb80add92889de2ee8 100644
--- a/src/nitro/room/object/logic/avatar/AvatarLogic.ts
+++ b/src/nitro/room/object/logic/avatar/AvatarLogic.ts
@@ -37,9 +37,6 @@ export class AvatarLogic extends MovingObjectLogic
     {
         super();
 
-        // System 11: avatars share the server's 500ms room walking cadence.
-        this.updateInterval = MovingObjectLogic.SHARED_MOVEMENT_INTERVAL;
-
         this._selected = false;
         this._reportedLocation = null;
         this._effectChangeTimeStamp = 0;
@@ -58,11 +55,6 @@ export class AvatarLogic extends MovingObjectLogic
         this._numberValueEndTimestamp = 0;
     }
 
-    protected get useSharedMovementCadence(): boolean
-    {
-        return true;
-    }
-
     public getEventTypes(): string[]
     {
         const types = [RoomObjectMouseEvent.CLICK, RoomObjectMouseEvent.DOUBLE_CLICK, RoomObjectMoveEvent.POSITION_CHANGED, RoomObjectMouseEvent.MOUSE_ENTER, RoomObjectMouseEvent.MOUSE_LEAVE, RoomObjectFurnitureActionEvent.MOUSE_BUTTON, RoomObjectFurnitureActionEvent.MOUSE_ARROW];
diff --git a/src/room/renderer/RoomSpriteCanvas.ts b/src/room/renderer/RoomSpriteCanvas.ts
index 9ebcd4e19d8e89fafa46c1f5bea546a8cf428314..9eb002c942fa71412a2b16b9239f7a89da8bf926 100644
--- a/src/room/renderer/RoomSpriteCanvas.ts
+++ b/src/room/renderer/RoomSpriteCanvas.ts
@@ -303,7 +303,7 @@ export class RoomSpriteCanvas implements IRoomRenderingCanvas
         // offset. Packet rollover may make the live tuple ambiguous, but does
         // not rewrite a proven relationship. DIVERGED is the only immediate
         // terminal transition and always returns movement to JOINER.
-        system11State: number, system11PreferredState: number, system11LastLoggedState: number, system11SharedPhase: number,
+        system11State: number, system11PreferredState: number, system11LastLoggedState: number, system11MasterPhase: number,
         system11MasterEdgeCapturedAt: number, system11JoinerEdgeCapturedAt: number,
         system11MasterCurrentSet: boolean, system11MasterPreviousSet: boolean,
         system11MasterCurrentSx: number, system11MasterCurrentSy: number, system11MasterCurrentSz: number,
@@ -1369,7 +1369,7 @@ export class RoomSpriteCanvas implements IRoomRenderingCanvas
                         ? (lateEntryAtEngage ? RoomSpriteCanvas.SYSTEM11_UNKNOWN : RoomSpriteCanvas.SYSTEM11_ALIGNED)
                         : RoomSpriteCanvas.SYSTEM11_BEHIND),
                     system11PreferredState: (lateEntryAtEngage ? RoomSpriteCanvas.SYSTEM11_BEHIND : RoomSpriteCanvas.SYSTEM11_ALIGNED),
-                    system11LastLoggedState: RoomSpriteCanvas.SYSTEM11_UNKNOWN, system11SharedPhase: 0,
+                    system11LastLoggedState: RoomSpriteCanvas.SYSTEM11_UNKNOWN, system11MasterPhase: 0,
                     system11MasterEdgeCapturedAt: NaN, system11JoinerEdgeCapturedAt: NaN,
                     system11MasterCurrentSet: false, system11MasterPreviousSet: false,
                     system11MasterCurrentSx: NaN, system11MasterCurrentSy: NaN, system11MasterCurrentSz: NaN,
@@ -1544,10 +1544,10 @@ export class RoomSpriteCanvas implements IRoomRenderingCanvas
                     const gy = m.gyy;
                     const gz = (m.sz + m.dz);
 
-                    // MovingObjectLogic puts every edge on the shared room
-                    // cadence. MASTER is only the convenient clock reader;
-                    // the resulting phase is common to both route edges.
-                    sync.system11SharedPhase = Math.min(1, Math.max(0, ((m.time - m.change) / m.interval)));
+                    // Keep native walking packet-anchored and smooth. System 11
+                    // reads MASTER's continuous phase only for the compatible
+                    // JOINER route edge; it does not retime either native walk.
+                    sync.system11MasterPhase = Math.min(1, Math.max(0, ((m.time - m.change) / m.interval)));
 
                     if(!sync.system11MasterCurrentSet)
                     {
@@ -3695,7 +3695,7 @@ export class RoomSpriteCanvas implements IRoomRenderingCanvas
                 // not spatially reinterpret a proven logical route offset.
                 if(!ended && !genuineDivergence)
                 {
-                    const sharedPhase = Math.min(1, Math.max(0, sync.system11SharedPhase));
+                    const masterPhase = Math.min(1, Math.max(0, sync.system11MasterPhase));
 
                     if((sync.system11State === RoomSpriteCanvas.SYSTEM11_ALIGNED) && mLoc)
                     {
@@ -3708,11 +3708,11 @@ export class RoomSpriteCanvas implements IRoomRenderingCanvas
                         && ((nowMs - sync.system11MasterEdgeCapturedAt) <= RoomSpriteCanvas.TRAILING_MASTER_EDGE_STALE_MS))
                     {
                         vx = (sync.system11MasterPreviousSx
-                            + ((sync.system11MasterPreviousGx - sync.system11MasterPreviousSx) * sharedPhase));
+                            + ((sync.system11MasterPreviousGx - sync.system11MasterPreviousSx) * masterPhase));
                         vy = (sync.system11MasterPreviousSy
-                            + ((sync.system11MasterPreviousGy - sync.system11MasterPreviousSy) * sharedPhase));
+                            + ((sync.system11MasterPreviousGy - sync.system11MasterPreviousSy) * masterPhase));
                         vz = (sync.system11MasterPreviousSz
-                            + ((sync.system11MasterPreviousGz - sync.system11MasterPreviousSz) * sharedPhase));
+                            + ((sync.system11MasterPreviousGz - sync.system11MasterPreviousSz) * masterPhase));
                         apply = true;
                         source = 'SYSTEM11_BEHIND';
                     }
@@ -3721,11 +3721,11 @@ export class RoomSpriteCanvas implements IRoomRenderingCanvas
                         && ((nowMs - sync.system11JoinerEdgeCapturedAt) <= RoomSpriteCanvas.TRAILING_MASTER_EDGE_STALE_MS))
                     {
                         vx = (sync.system11JoinerCurrentSx
-                            + ((sync.system11JoinerCurrentGx - sync.system11JoinerCurrentSx) * sharedPhase));
+                            + ((sync.system11JoinerCurrentGx - sync.system11JoinerCurrentSx) * masterPhase));
                         vy = (sync.system11JoinerCurrentSy
-                            + ((sync.system11JoinerCurrentGy - sync.system11JoinerCurrentSy) * sharedPhase));
+                            + ((sync.system11JoinerCurrentGy - sync.system11JoinerCurrentSy) * masterPhase));
                         vz = (sync.system11JoinerCurrentSz
-                            + ((sync.system11JoinerCurrentGz - sync.system11JoinerCurrentSz) * sharedPhase));
+                            + ((sync.system11JoinerCurrentGz - sync.system11JoinerCurrentSz) * masterPhase));
                         apply = true;
                         source = 'SYSTEM11_AHEAD';
                     }
